let userEmail = 'lcoabc';
let password = '1234'


// read MDN string resources

// toUpperCase(), toLowerCase(), trim()

// trim() removes the empty spaces at the starting of the  string
 
// includes()

let userChecker = function (myString){
    if ((myString.includes((123)) && (myString.length > 6) )) {
        return true
         // only one return statement works in a function
    }     // else  here is completely optional
        return false // only onw of them is going to return...
          // if true is detected then control will move out of the function
          // else false will be detceted
   
}

