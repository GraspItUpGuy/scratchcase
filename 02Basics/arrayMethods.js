// visit after marvels.js

const numbers = ['one', 'two', 'three', 'four', 'five', 'six' ];

// numbers[1] = 'Something'
// console.log(numbers[1] );


//start

    // number shift

    // numbers.shift()  // also returns the element that it is shifting
    //  console.log(numbers.length)
    // console.log(numbers.shift())
    // console.log(numbers); // first element is gone
    
    // numbers unshift

    // numbers.unshift('Something')  // inserting elemnt at index 0
    
    // console.log(numbers)
 

//end



//numbers.pop()// removes the last elemnt from the array and also returns the value
              // that it is popping
//               console.log('the number to be deleted is ' + numbers.pop())
   
// console.log(numbers)

// numbers.push('Seven') // pushes 7 at the end of the array
// console.log(numbers)


// middle



// numbers.splice (2,1,'Something' ) // 3arguments

// // starting positon, number of elements to be deleated,  the value to be added
// can only add one value, not more than that

// console.log(numbers)

numbers.splice(3, 3, 'hohoho', )
console.log(numbers);


